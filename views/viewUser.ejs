<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wedding Panel | Couple Details</title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/dayjs@1/dayjs.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&display=swap');

    body {
      font-family: 'Plus Jakarta Sans', sans-serif;
    }

    h1,
    h2,
    h3,
    h4,
    .serif-font {
      font-family: 'Playfair Display', serif;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.5);
    }
  </style>
</head>

<body class="bg-[#faf7f6] text-slate-800">
  <div class="flex h-screen overflow-hidden selection:bg-[#FF5E62] selection:text-white">
    <%- include('partials/sidebar') %>

      <div class="flex flex-col flex-1 overflow-x-hidden overflow-y-auto relative">
        <%- include('partials/header') %>

          <main class="w-full max-w-[1920px] mx-auto p-4 md:p-6 lg:p-8">

            <!-- Navigation & Actions -->
            <div class="flex items-center justify-between mb-8">
              <a href="/users"
                class="group flex items-center text-sm font-semibold text-slate-500 hover:text-[#FF5E62] transition-colors">
                <div
                  class="w-8 h-8 rounded-full bg-white border border-slate-200 flex items-center justify-center mr-2 group-hover:border-[#FF5E62] group-hover:bg-[#FF5E62] group-hover:text-white transition-all shadow-sm">
                  <i class="fas fa-arrow-left"></i>
                </div>
                Back to Couples
              </a>
            </div>

            <div class="grid grid-cols-1 xl:grid-cols-3 gap-8">
              <!-- Left Column: Couple Profile -->
              <div class="xl:col-span-1 space-y-6">

                <!-- Main Profile Card -->
                <div
                  class="bg-white rounded-[2rem] shadow-xl shadow-slate-200/50 border border-slate-100 overflow-hidden relative">
                  <!-- Decorative Pattern -->
                  <div
                    class="absolute top-0 left-0 w-full h-32 bg-gradient-to-br from-[#F3C7BF] to-[#FF5E62] opacity-90">
                  </div>
                  <div
                    class="absolute top-0 left-0 w-full h-32 bg-[url('https://www.transparenttextures.com/patterns/cubes.png')] opacity-20">
                  </div>

                  <div class="relative px-6 pt-16 pb-6 text-center">
                    <!-- Avatar -->
                    <div class="relative inline-block mx-auto mb-4">
                      <% if (user.profileImagePath) { %>
                        <img src="<%= user.profileImagePath %>" alt="Couple"
                          class="w-24 h-24 md:w-32 md:h-32 rounded-full object-cover border-4 border-white shadow-lg">
                        <% } else { %>
                          <div
                            class="w-24 h-24 md:w-32 md:h-32 rounded-full bg-gradient-to-br from-slate-100 to-slate-200 border-4 border-white shadow-lg flex items-center justify-center text-3xl font-bold text-slate-400">
                            <i class="fas fa-user-friends"></i>
                          </div>
                          <% } %>
                            <div
                              class="absolute bottom-1 right-1 bg-emerald-500 w-5 h-5 md:w-6 md:h-6 rounded-full border-2 border-white"
                              title="Active"></div>
                    </div>

                    <!-- Names & Title -->
                    <h1 class="text-2xl md:text-3xl font-bold text-slate-800 mb-1">
                      <%= user.partner1Name || 'Partner 1' %> <span class="text-[#FF5E62]">&amp;</span>
                        <%= user.partner2Name || 'Partner 2' %>
                    </h1>
                    <p class="text-sm text-slate-500 font-medium mb-6 uppercase tracking-wider">
                      <%= user.userType || 'Couple' %> Profile
                    </p>

                    <!-- Quick Actions -->
                    <div class="flex justify-center gap-3 mb-8">
                      <a href="mailto:<%= user.email %>"
                        class="px-4 py-2 bg-slate-50 text-slate-600 rounded-xl text-sm font-semibold hover:bg-[#FF5E62] hover:text-white transition-colors">
                        <i class="far fa-envelope mr-2"></i> Email
                      </a>
                    </div>

                    <!-- Info Grid -->
                    <div class="grid grid-cols-1 gap-4 text-left">
                      <div class="p-4 rounded-2xl bg-slate-50 border border-slate-100/50">
                        <div class="flex items-start gap-3">
                          <div class="p-2 bg-white rounded-lg text-[#FF5E62] shadow-sm">
                            <i class="far fa-envelope"></i>
                          </div>
                          <div>
                            <p class="text-xs font-bold text-slate-400 uppercase">Email Address</p>
                            <p class="text-sm font-semibold text-slate-800 break-all">
                              <%= user.email %>
                            </p>
                          </div>
                        </div>
                      </div>

                      <div class="p-4 rounded-2xl bg-slate-50 border border-slate-100/50">
                        <div class="flex items-start gap-3">
                          <div class="p-2 bg-white rounded-lg text-[#FF5E62] shadow-sm">
                            <i class="far fa-calendar-alt"></i>
                          </div>
                          <div>
                            <p class="text-xs font-bold text-slate-400 uppercase">Wedding Date</p>
                            <p class="text-sm font-semibold text-slate-800">
                              <%= user.weddingDate ? new Date(user.weddingDate).toLocaleDateString('en-US', {
                                weekday: 'long' , year: 'numeric' , month: 'long' , day: 'numeric' }) : 'Not set' %>
                            </p>
                          </div>
                        </div>
                      </div>

                      <div class="p-4 rounded-2xl bg-slate-50 border border-slate-100/50">
                        <div class="flex items-start gap-3">
                          <div class="p-2 bg-white rounded-lg text-[#FF5E62] shadow-sm">
                            <i class="fas fa-map-marker-alt"></i>
                          </div>
                          <div>
                            <p class="text-xs font-bold text-slate-400 uppercase">Venue</p>
                            <p class="text-sm font-semibold text-slate-800">
                              <%= user.venue || 'Not set' %>
                            </p>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>

                  <!-- Story Section -->
                  <div class="px-6 py-6 border-t border-slate-100 bg-slate-50/50">
                    <h3 class="text-lg font-bold text-slate-800 mb-3 flex items-center">
                      <i class="fas fa-heart text-[#FF5E62] mr-2"></i> Our Story
                    </h3>
                    <p class="text-sm text-slate-600 leading-relaxed italic">
                      "<%= user.story || 'No story shared yet.' %>"
                    </p>
                  </div>
                </div>

                <!-- System Meta -->
                <div class="bg-white rounded-[1.5rem] shadow-lg shadow-slate-200/50 border border-slate-100 p-6">
                  <h3 class="text-sm font-bold text-slate-800 mb-4 uppercase tracking-wider">System Information</h3>
                  <div class="space-y-3 text-xs">
                    <div class="flex justify-between py-2 border-b border-slate-50">
                      <span class="text-slate-500">Couple ID</span>
                      <span class="font-mono text-slate-700 select-all">
                        <%= user.id %>
                      </span>
                    </div>
                    <div class="flex justify-between py-2 border-b border-slate-50">
                      <span class="text-slate-500">Created At</span>
                      <span class="font-medium text-slate-700">
                        <%= user.createdAt && user.createdAt._seconds ? new Date(user.createdAt._seconds *
                          1000).toLocaleString() : 'N/A' %>
                      </span>
                    </div>
                    <div class="flex justify-between py-2">
                      <span class="text-slate-500">Status</span>
                      <span
                        class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-50 text-emerald-600 uppercase">Active</span>
                    </div>
                  </div>
                </div>

              </div>

              <!-- Right Column: Events -->
              <div class="xl:col-span-2">
                <div
                  class="bg-white rounded-[2rem] shadow-xl shadow-slate-200/50 border border-slate-100 overflow-hidden min-h-[600px] flex flex-col">
                  <div
                    class="px-8 py-6 border-b border-slate-100 bg-gradient-to-r from-white to-slate-50/50 flex items-center justify-between">
                    <div>
                      <h2 class="text-2xl font-bold text-slate-800">Wedding Events</h2>
                      <p class="text-sm text-slate-400 font-medium mt-1">Timeline of celebrations</p>
                    </div>
                    <span class="bg-[#F3C7BF]/20 text-[#FF5E62] px-3 py-1 rounded-full text-xs font-bold">
                      <%= events ? events.length : 0 %> Events
                    </span>
                  </div>

                  <div class="p-8 bg-slate-50/30 flex-1">
                    <% if (!events || events.length===0) { %>
                      <div class="h-full flex flex-col items-center justify-center text-slate-400 py-20">
                        <div class="w-20 h-20 bg-slate-100 rounded-full flex items-center justify-center mb-4">
                          <i class="far fa-calendar-times text-4xl text-slate-300"></i>
                        </div>
                        <h3 class="text-lg font-bold text-slate-600">No Events Found</h3>
                        <p class="text-sm">This couple hasn't created any events yet.</p>
                      </div>
                      <% } else { %>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                          <% events.forEach(event=> { %>
                            <div
                              class="group bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-xl transition-all duration-300 border border-slate-100 hover:border-[#F3C7BF]/50 flex flex-col h-full hover:-translate-y-1">
                              <!-- Cover Image -->
                              <div class="aspect-video relative overflow-hidden bg-slate-200">
                                <% if (event.coverPhotoPath) { %>
                                  <!-- Displaying whatever path is provided; formatted if needed or raw -->
                                  <img src="<%= event.coverPhotoPath %>" alt="Event Cover"
                                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700">
                                  <% } else { %>
                                    <div
                                      class="w-full h-full flex items-center justify-center bg-gradient-to-br from-[#F3C7BF] to-[#FF5E62] opacity-80">
                                      <i class="fas fa-glass-cheers text-4xl text-white"></i>
                                    </div>
                                    <% } %>

                                      <!-- Status Badge -->
                                      <div class="absolute top-3 right-3">
                                        <% if (event.isPublished) { %>
                                          <span
                                            class="px-2.5 py-1 rounded-lg bg-emerald-500/90 backdrop-blur-sm text-white text-[10px] font-bold uppercase shadow-sm tracking-wider">
                                            Published
                                          </span>
                                          <% } else { %>
                                            <span
                                              class="px-2.5 py-1 rounded-lg bg-amber-500/90 backdrop-blur-sm text-white text-[10px] font-bold uppercase shadow-sm tracking-wider">
                                              Draft
                                            </span>
                                            <% } %>
                                      </div>
                              </div>

                              <!-- Content -->
                              <div class="p-5 flex flex-col flex-1">
                                <div class="flex items-center justify-between mb-3">
                                  <span
                                    class="text-xs font-bold text-[#FF5E62] uppercase tracking-wider bg-[#FF5E62]/5 px-2 py-1 rounded">Event</span>
                                  <!-- Date badge -->
                                  <span class="text-xs font-semibold text-slate-500 flex items-center">
                                    <i class="far fa-clock mr-1"></i>
                                    <%= event.weddingDate ? new Date(event.weddingDate._seconds ?
                                      event.weddingDate._seconds * 1000 : event.weddingDate).toLocaleDateString()
                                      : 'Date TBD' %>
                                  </span>
                                </div>

                                <h3
                                  class="text-xl font-bold text-slate-800 mb-2 font-serif group-hover:text-[#FF5E62] transition-colors">
                                  <%= event.brideName || 'Bride' %> <span class="text-slate-300 mx-1">&amp;</span>
                                    <%= event.groomName || 'Groom' %>
                                </h3>

                                <div class="mb-4">
                                  <div class="flex items-center text-sm text-slate-500 mb-1">
                                    <i class="fas fa-map-marker-alt w-5 text-center mr-2 text-slate-400"></i>
                                    <span class="truncate">
                                      <%= event.venue || 'Venue TBD' %>
                                    </span>
                                  </div>
                                </div>

                                <div
                                  class="border-t border-slate-50 pt-3 mt-auto flex items-center justify-between gap-3">
                                  <p class="text-sm text-slate-500 line-clamp-2 italic flex-1">
                                    "<%= event.description || 'No description provided.' %>"
                                  </p>
                                  <a href="/users/viewEvent/<%= event.id %>"
                                    class="flex-shrink-0 w-8 h-8 rounded-full bg-slate-50 hover:bg-[#FF5E62] text-slate-400 hover:text-white flex items-center justify-center transition-all duration-300 shadow-sm"
                                    title="View Event Details">
                                    <i class="fas fa-arrow-right text-xs"></i>
                                  </a>
                                </div>
                              </div>
                            </div>
                            <% }); %>
                        </div>
                        <% } %>
                  </div>
                </div>
              </div>
            </div>

          </main>
      </div>
  </div>

  <%- include('partials/script') %>
</body>

</html>