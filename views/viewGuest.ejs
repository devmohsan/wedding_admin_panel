<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wedding Panel | Guest Details</title>
    <link href="/css/output.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/dayjs@1/dayjs.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&display=swap');

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
        }

        /* h1,
        h2,
        h3,
        h4,
        .serif-font {
            font-family: 'Playfair Display', serif;
        } */
    </style>
</head>

<body class="bg-[#faf7f6] text-slate-800">
    <div class="flex h-screen overflow-hidden selection:bg-[#FF5E62] selection:text-white">
        <%- include('partials/sidebar') %>

            <div class="flex flex-col flex-1 overflow-x-hidden overflow-y-auto relative">
                <%- include('partials/header') %>

                    <main class="w-full max-w-[1920px] mx-auto p-4 md:p-6 lg:p-8">

                        <!-- Navigation -->
                        <div class="flex items-center justify-between mb-8">
                            <a href="/users/guests"
                                class="group flex items-center text-sm font-semibold text-slate-500 hover:text-[#FF5E62] transition-colors">
                                <div
                                    class="w-8 h-8 rounded-full bg-white border border-slate-200 flex items-center justify-center mr-2 group-hover:border-[#FF5E62] group-hover:bg-[#FF5E62] group-hover:text-white transition-all shadow-sm">
                                    <i class="fas fa-arrow-left"></i>
                                </div>
                                Back to Guests
                            </a>
                        </div>

                        <div class="max-w-4xl mx-auto">
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">

                                <!-- Left Column: Guest Profile Card -->
                                <div class="md:col-span-1 space-y-6">
                                    <div
                                        class="bg-white rounded-[2rem] shadow-xl shadow-slate-200/50 border border-slate-100 overflow-hidden relative">
                                        <!-- Decorative Header -->
                                        <div
                                            class="absolute top-0 left-0 w-full h-24 bg-gradient-to-br from-[#F3C7BF] to-[#FF5E62] opacity-90">
                                        </div>

                                        <div class="relative px-6 pt-12 pb-6 text-center">
                                            <!-- Avatar -->
                                            <div class="relative inline-block mx-auto mb-4">
                                                <div
                                                    class="w-24 h-24 md:w-28 md:h-28 rounded-full bg-white border-4 border-white shadow-lg flex items-center justify-center text-4xl font-bold text-[#FF5E62] overflow-hidden">
                                                    <% if (user.profileImagePath) { %>
                                                        <img src="<%= user.profileImagePath %>" alt="Guest"
                                                            class="w-full h-full object-cover">
                                                        <% } else { %>
                                                            <%= user.fullName ? user.fullName.charAt(0).toUpperCase()
                                                                : 'G' %>
                                                                <% } %>
                                                </div>
                                            </div>

                                            <!-- Name & Role -->
                                            <h1 class="text-xl md:text-2xl font-bold text-slate-800 mb-1">
                                                <%= user.fullName || 'Guest Name' %>
                                            </h1>
                                            <span
                                                class="inline-block px-3 py-1 rounded-full bg-slate-100 text-slate-500 text-xs font-bold uppercase tracking-wider mb-6">
                                                <%= user.userType || 'Guest' %>
                                            </span>

                                            <!-- Quick Actions -->
                                            <div class="flex justify-center gap-3">
                                                <% if (user.email) { %>
                                                    <a href="mailto:<%= user.email %>"
                                                        class="w-10 h-10 rounded-full bg-slate-50 hover:bg-[#FF5E62] text-slate-500 hover:text-white flex items-center justify-center transition-all shadow-sm"
                                                        title="Send Email">
                                                        <i class="far fa-envelope"></i>
                                                    </a>
                                                    <% } %>
                                                        <% if (user.phoneNumber) { %>
                                                            <a href="tel:<%= user.phoneNumber %>"
                                                                class="w-10 h-10 rounded-full bg-slate-50 hover:bg-[#FF5E62] text-slate-500 hover:text-white flex items-center justify-center transition-all shadow-sm"
                                                                title="Call User">
                                                                <i class="fas fa-phone"></i>
                                                            </a>
                                                            <% } %>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Status Card -->
                                    <div class="bg-white rounded-[1.5rem] shadow-lg border border-slate-100 p-6">
                                        <h3 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-4">
                                            System Status</h3>
                                        <div class="flex items-center justify-between">
                                            <span class="text-sm font-medium text-slate-600">Account Status</span>
                                            <% if (user.status==='suspended' ) { %>
                                                <span
                                                    class="px-2 py-1 rounded-md bg-red-50 text-red-600 text-xs font-bold uppercase border border-red-100">Suspended</span>
                                                <% } else { %>
                                                    <span
                                                        class="px-2 py-1 rounded-md bg-emerald-50 text-emerald-600 text-xs font-bold uppercase border border-emerald-100">Active</span>
                                                    <% } %>
                                        </div>
                                    </div>
                                </div>

                                <!-- Right Column: Details & Info -->
                                <div class="md:col-span-2 space-y-6">

                                    <!-- Contact & Personal Info -->
                                    <div class="bg-white rounded-[2rem] shadow-xl border border-slate-100 p-8">
                                        <h2 class="text-xl font-bold text-slate-800 mb-6 flex items-center">
                                            <i class="far fa-id-card text-[#FF5E62] mr-3"></i> Guest Information
                                        </h2>

                                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                            <div class="p-4 rounded-xl bg-slate-50 border border-slate-100/50">
                                                <p
                                                    class="text-[10px] font-bold text-slate-400 uppercase tracking-wide mb-1">
                                                    Email Address</p>
                                                <p class="text-sm font-semibold text-slate-800 break-all">
                                                    <%= user.email || 'Not provided' %>
                                                </p>
                                            </div>

                                            <div class="p-4 rounded-xl bg-slate-50 border border-slate-100/50">
                                                <p
                                                    class="text-[10px] font-bold text-slate-400 uppercase tracking-wide mb-1">
                                                    Phone Number</p>
                                                <p class="text-sm font-semibold text-slate-800">
                                                    <%= user.phoneNumber || 'Not provided' %>
                                                </p>
                                            </div>

                                            <div
                                                class="p-4 rounded-xl bg-slate-50 border border-slate-100/50 md:col-span-2">
                                                <p
                                                    class="text-[10px] font-bold text-slate-400 uppercase tracking-wide mb-2">
                                                    About / Bio</p>
                                                <p class="text-sm text-slate-600 italic leading-relaxed">
                                                    "<%= user.bio || 'No information provided.' %>"
                                                </p>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Boked Events & Tickets -->
                                    <div class="bg-white rounded-[2rem] shadow-xl border border-slate-100 p-8">
                                        <h2 class="text-xl font-bold text-slate-800 mb-6 flex items-center">
                                            <i class="fas fa-calendar-check text-[#FF5E62] mr-3"></i> Booked Events
                                        </h2>

                                        <% if (typeof bookingsWithDetails !=='undefined' && bookingsWithDetails.length>
                                            0) { %>
                                            <% bookingsWithDetails.forEach(detail=> { %>
                                                <div
                                                    class="mb-6 last:mb-0 p-5 rounded-2xl bg-slate-50/80 border border-slate-100">
                                                    <!-- Event Info -->
                                                    <div
                                                        class="flex flex-col md:flex-row md:items-center justify-between gap-4 mb-4 border-b border-slate-200/60 pb-4">
                                                        <div>
                                                            <span
                                                                class="text-xs font-bold text-[#FF5E62] uppercase tracking-wider mb-1 block">Event</span>
                                                            <h3 class="text-lg font-bold text-slate-800">
                                                                <a href="/users/viewEvent/<%= detail.event.id %>"
                                                                    class="hover:text-[#FF5E62] transition-colors">
                                                                    <%= detail.event.brideName %> & <%=
                                                                            detail.event.groomName %>
                                                                </a>
                                                            </h3>
                                                            <p class="text-xs text-slate-500 font-medium mt-1">
                                                                <i class="far fa-clock mr-1"></i>
                                                                <%= detail.event.weddingDate ? new
                                                                    Date(detail.event.weddingDate._seconds ?
                                                                    detail.event.weddingDate._seconds * 1000 :
                                                                    detail.event.weddingDate).toLocaleDateString()
                                                                    : 'Date TBD' %>
                                                            </p>
                                                        </div>
                                                        <div class="text-right">
                                                            <span
                                                                class="inline-block px-3 py-1 bg-emerald-100 text-emerald-600 rounded-full text-xs font-bold uppercase">
                                                                Booked
                                                            </span>
                                                        </div>
                                                    </div>

                                                    <!-- Tickets List -->
                                                    <div>
                                                        <p
                                                            class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-3">
                                                            Tickets Purchased</p>
                                                        <% if (detail.tickets && detail.tickets.length> 0) { %>
                                                            <div class="grid grid-cols-1 gap-3">
                                                                <% detail.tickets.forEach(ticket=> { %>
                                                                    <div
                                                                        class="flex items-center justify-between bg-white p-3 rounded-xl border border-slate-100 shadow-sm">
                                                                        <div class="flex items-center gap-3">
                                                                            <div
                                                                                class="w-8 h-8 rounded-full bg-[#F3C7BF]/20 flex items-center justify-center text-[#FF5E62]">
                                                                                <i
                                                                                    class="fas fa-ticket-alt text-xs"></i>
                                                                            </div>
                                                                            <span
                                                                                class="text-sm font-semibold text-slate-700">
                                                                                <%= ticket.title || ticket.name %>
                                                                            </span>
                                                                        </div>
                                                                        <div class="flex items-center gap-2">
                                                                            <span
                                                                                class="text-xs text-slate-400">Qty:</span>
                                                                            <span
                                                                                class="text-sm font-bold text-slate-800 bg-slate-50 px-2 py-0.5 rounded border border-slate-100">
                                                                                <%= ticket.bookedQuantity %>
                                                                            </span>
                                                                        </div>
                                                                    </div>
                                                                    <% }); %>
                                                            </div>
                                                            <% } else { %>
                                                                <p class="text-xs text-slate-400 italic">No specific
                                                                    ticket details found.</p>
                                                                <% } %>
                                                    </div>
                                                </div>
                                                <% }); %>
                                                    <% } else { %>
                                                        <div
                                                            class="p-8 rounded-2xl bg-slate-50 border border-slate-100 text-center">
                                                            <div
                                                                class="w-12 h-12 rounded-full bg-slate-200 flex items-center justify-center mx-auto mb-3 text-slate-400">
                                                                <i class="far fa-calendar-times text-xl"></i>
                                                            </div>
                                                            <p class="text-sm text-slate-500 font-medium">No bookings
                                                                found for this guest.</p>
                                                        </div>
                                                        <% } %>
                                    </div>

                                    <!-- Meta Data -->
                                    <div class="bg-slate-900 rounded-[1.5rem] p-6 text-white relative overflow-hidden">
                                        <div
                                            class="absolute top-0 right-0 w-40 h-40 bg-[#FF5E62] rounded-full blur-[80px] opacity-20 -mr-10 -mt-10">
                                        </div>
                                        <div class="relative z-10 grid grid-cols-2 gap-4">
                                            <div>
                                                <span class="block text-slate-400 text-xs mb-1">Guest ID</span>
                                                <span class="font-mono text-xs text-slate-200">
                                                    <%= user.userId || user.id %>
                                                </span>
                                            </div>
                                            <div>
                                                <span class="block text-slate-400 text-xs mb-1">Joined Date</span>
                                                <span class="text-sm font-medium">
                                                    <%= user.createdAt ? new Date(user.createdAt._seconds ?
                                                        user.createdAt._seconds * 1000 :
                                                        user.createdAt).toLocaleDateString() : 'N/A' %>
                                                </span>
                                            </div>
                                        </div>
                                    </div>

                                </div>
                            </div>
                        </div>

                    </main>
            </div>
    </div>

    <%- include('partials/script') %>
</body>

</html>